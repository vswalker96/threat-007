build:
  docker:
    backend: Dockerfile.backend
    frontend: Dockerfile.frontend
    threat-engine: Dockerfile.threat-engine
  config:
    BACKEND_PORT: 8080
    FRONTEND_PORT: 80
    THREAT_ENGINE_PORT: 8000

run:
  web: docker-compose up